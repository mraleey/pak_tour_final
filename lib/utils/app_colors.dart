import 'package:flutter/material.dart';

class AppColors {
  // Primary color - Main brand color
  static const Color primaryColor = Color(0xFFcfa408);

  // Accent color - Complementary to primary
  static const Color accentColor = Color(0xFF006064);

  // Background color - Main screen background
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // Surface color - Cards, dialogs, etc.
  static const Color surfaceColor = Color(0xFFFFFFFF);

  // Error color - Errors, alerts
  static const Color errorColor = Color(0xFFB00020);

  // Success color - Successful actions
  static const Color successColor = Color(0xFF4CAF50);

  // Info color - Information messages
  static const Color infoColor = Color(0xFF2196F3);

  // Warning color - Warning messages
  static const Color warningColor = Color(0xFFFFC107);

  // Text colors
  static const Color textPrimaryColor = Color(0xFF212121);
  static const Color textSecondaryColor = Color(0xFF757575);
  static const Color textLightColor = Color(0xFFFFFFFF);

  // Icon colors
  static const Color iconPrimaryColor = Color(0xFF212121);
  static const Color iconSecondaryColor = Color(0xFF757575);
  static const Color iconLightColor = Color(0xFFFFFFFF);

  // Border colors
  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color dividerColor = Color(0xFFEEEEEE);

  // Custom app specific colors
  static const Color visitedColor = Color(0xFF4CAF50);
  static const Color notVisitedColor = Color(0xFFBDBDBD);
  static const Color wishlistColor = Color(0xFFF44336);
  static const Color routeColor = Color(0xFF3F51B5);
  static const Color chatBubbleUserColor = Color(0xFFE3F2FD);
  static const Color chatBubbleAssistantColor = Color(0xFFE8F5E9);
}
